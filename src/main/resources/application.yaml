spring:
    application:
        name: farmacia_api
    profiles:
        active: dev

server:
    port: ${SERVER_PORT:8080}

springdoc:
    api-docs:
        enabled: false
        path: /api-docs
    swagger-ui:
        enabled: false
        path: /swagger-ui.html

